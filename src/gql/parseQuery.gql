query ParseQuery($feed: String!) {
  podcast(feed: $feed) {
    title
    description
    subtitle
    artwork
    episodes {
      id
      url
      published
      title
      shownotes
    }
  }
}
