query ParseQuery($feed: String!) {
  podcast(feed: $feed) {
    title
    author
    description
    subtitle
    artwork
    episodes {
      id
      url
      published
      title
      shownotes
    }
  }
}
